%table
  %tbody
    - collection.each do |project|
      %tr
        %td
          .project_name= project_link project
          - if show_access
            .role= '(' + project_access_level(project) + ')'
        %td= project.owner_names
