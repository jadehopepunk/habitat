= top_available_heading "Soil Test on #{@soil_test.tested_on.to_s(:long)}"
.row
  .span10
  .span6
    %table
      %thead
        %tbody
          = attribute_row(@soil_test, :tested_on)
          = attribute_row(@soil_test, :reference)
          = attribute_row(@soil_test, :area)
    = render :partial => 'site_analysis/soil_tests/soil_test_results/table', :locals => {:collection => @soil_test.soil_test_results}