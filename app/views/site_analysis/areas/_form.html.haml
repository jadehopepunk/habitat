= simple_form_for [:site_analysis, @project, resource], :wrapper => :inline do |f|
  = form_errors(f)
  .inputs
    = f.input :name, :input_html => { :class => "span8" }
    = f.input :description, :input_html => { :class => "span8", :rows => 3 }
  = form_actions(f)
